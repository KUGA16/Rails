<!-- 編集 -->

<h1><storong>Editing Book</storong></h1>


<%= form_for(@book) do |f| %>
	<% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this book from being saved:
    	<ul>
       		<% @book.errors.full_messages.each do |message| %>
        	<li><%= message %></li>
    	</ul>
        <% end %>
    <% end %>


<div class="field">
	<h4>Title</h4>
	<%= f.text_field :title %>
</div>

<div class="field">
	<h4>Body</h4>
	<%= f.text_area :body %>
</div>

<div class="action"><%= f.submit"Update Book" %></div>

<div><%= link_to "Show", book_path %>|<%= link_to "Back", books_path %></div>

<% end %>





